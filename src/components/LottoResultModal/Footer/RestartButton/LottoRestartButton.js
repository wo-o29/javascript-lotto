const $LottoRestartButton = () => {
  return createElement("button", {
    type: "button",
    class: "lotto-restart-button",
    textContent: "다시 시작하기",
  });
};

export default $LottoRestartButton;
